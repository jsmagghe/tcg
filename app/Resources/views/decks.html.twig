{% extends "::base.html.twig" %}

{% block stylesheets %}
    {{ parent() }}

    <link type="text/css" rel="stylesheet" href="{{ asset('css/carte.css') }}" />
{% endblock %}



{% block body %}
    {{ parent() }}
    <div class="col-lg-12 col-md-8">
        <div class="row" id="decks">
            <a href="{{ path('jeus_'~jeu~'_'~nom~'_creer')}}">Créer un deck</a>

            {% if liste|length==0 %}
                aucun deck disponible
            {% else %}
                {% for Deck in liste %}
                    <p>
                        <a href="{{ path('jeus_'~jeu~'_deck_editer', {'id':Deck.id})}}">{{ Deck.nom }}</a>
                    </p>
                {% endfor %}
            {% endif %}        
        </div>
    </div>
</div>


{%endblock %}
{% block javascripts %}
    {{ parent() }}
    <script>
        var url = {{ path('jeus_'~jeu~'_carte') }};
    </script>
{% endblock %}
